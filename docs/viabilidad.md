# Viabilidad técnica del "Mapa de patrones de scroll" para una app de bienestar digital para adolescentes (13-18)

## TL;DR
- **La suposición central de la spec es correcta a medias y debe matizarse**: en **Android** sÃ­ se puede clasificar el uso "por app completa" (Instagram, TikTok, YouTube como unidades) con `UsageStatsManager` + el permiso `PACKAGE_USAGE_STATS`; pero en **iOS no existe** una API pÃºblica que entregue datos crudos de uso a una app de auto-conocimiento, y **ninguna red social** ofrece una API en tiempo real para leer cuÃ¡nto tiempo o quÃ© contenido consume el usuario.
- **No hay granularidad de "tipo de contenido" dentro de una app** (Reels vs Stories vs DMs) por ninguna vÃ­a estÃ¡ndar del SO: tanto Android como iOS miden Ãºnicamente a nivel de app/paquete completo. Inferir contenido intra-app solo es posible con `AccessibilityService` en Android, lo que conlleva alto riesgo de rechazo y de baneo en Google Play.
- **Enfoque más viable y "creíble" para el prototipo de formación**: Android `UsageStatsManager` real (tiempo por app, desbloqueos, franjas horarias) + clasificaciÃ³n de apps por nombre de paquete como *proxy* de "tipo de plataforma", complementado opcionalmente con **importaciÃ³n manual de exports GDPR** (Instagram "Descarga tu informaciÃ³n" JSON, TikTok "Download your data" JSON) como fuente de datos reales histÃ³ricos, dejando explÃ­cito que no hay granularidad intra-app ni tiempo real.

## Key Findings

### 1. Android: SÃ es viable a nivel de app completa, con permiso especial
- `UsageStatsManager` (introducido en API 21 / Android 5.0 Lollipop) devuelve tiempo en *foreground* por paquete, agregado por intervalos. La documentaciÃ³n de Android especifica que los datos se agregan en "days, weeks, months, and years", y en la prÃ¡ctica se puede recuperar uso hasta **7 dÃ­as** con intervalo diario, **~4 semanas** semanal, **6 meses** mensual y **2 aÃ±os** anual; para `queryEvents`, la propia doc advierte que "Events are only kept by the system for a few days" (retenciÃ³n de eventos muy corta).
- VÃ­a `queryEvents` / `UsageEvents.Event` se obtienen eventos con *timestamp*: `ACTIVITY_RESUMED`/`ACTIVITY_PAUSED` (foreground/background), `SCREEN_INTERACTIVE`/`SCREEN_NON_INTERACTIVE`, `KEYGUARD_SHOWN`/`KEYGUARD_HIDDEN` (bloqueo/desbloqueo â†’ conteo de desbloqueos), `NOTIFICATION_SEEN` (cÃ³digo 10) y `NOTIFICATION_INTERRUPTION` (cÃ³digo 12). Con esto se reconstruyen sesiones, desbloqueos y franjas horarias.
- **Permiso**: `PACKAGE_USAGE_STATS` es de nivel sistema ("special access"). La nota literal de la documentaciÃ³n oficial es: *"Most methods on this API require the permission android.permission.PACKAGE_USAGE_STATS. However, declaring the permission implies intention to use the API and the user of the device still needs to grant permission through the Settings application. See android.provider.Settings#ACTION_USAGE_ACCESS_SETTINGS."* Es decir: no se concede en *runtime*; el usuario debe activarlo manualmente en Ajustes vÃ­a `Settings.ACTION_USAGE_ACCESS_SETTINGS`.
- **PolÃ­tica Google Play**: el Usage Access estÃ¡ permitido pero cae bajo la polÃ­tica "Permissions and APIs that Access Sensitive Information": el permiso debe ser necesario para la funcionalidad nÃºcleo promocionada, con divulgaciÃ³n prominente y consentimiento afirmativo del usuario.
- **LimitaciÃ³n clave (refuta parte de la spec)**: NO desglosa el uso por tipo de contenido dentro de la app. No distingue Reels de Stories de DMs dentro de Instagram. Solo llega a nivel de paquete + clase de actividad (`getClassName`), insuficiente para inferir tipo de contenido de forma fiable.

### 2. iOS: NO hay API pÃºblica equivalente para auto-analÃ­tica en el propio dispositivo
- Apple expone la Screen Time API (iOS 15/16) mediante tres frameworks: `FamilyControls`, `ManagedSettings` y `DeviceActivity`. Requieren el **entitlement Family Controls**, que es privilegiado y requiere aprobaciÃ³n explÃ­cita de Apple para publicar en TestFlight/App Store; estÃ¡ diseÃ±ado para control parental.
- Desde **iOS 16** existe la autorizaciÃ³n `.individual` (`AuthorizationCenter.shared.requestAuthorization(for: .individual)`), que permite auto-monitorizaciÃ³n sin Family Sharing y sin control parental â€” esta es la vÃ­a teÃ³rica mÃ¡s cercana al caso de uso de un adolescente monitorizÃ¡ndose a sÃ­ mismo. En la WWDC22 Apple lo describiÃ³ como: *"individual authorization can be used by any number of apps per deviceâ€¦ since individual authorization is not for parental control use cases, implicit restrictions for iCloud sign-out and deletion of an app do not apply."* No obstante, esta autorizaciÃ³n individual puede ser desactivada trivialmente por el usuario desde los ajustes de la app.
- **RestricciÃ³n decisiva**: los datos de uso solo son accesibles dentro de una `DeviceActivityReport` extension, fuertemente aislada â€” sin acceso a red y sin poder mover datos crudos fuera de su *address space*. La app anfitriona **no recibe datos crudos**; solo puede renderizar una vista (SwiftUI) que el sistema rellena. AdemÃ¡s, la selecciÃ³n de apps se hace vÃ­a `FamilyActivityPicker`, que devuelve **tokens opacos** no identificables ni desencriptables: la app ni siquiera sabe quÃ© app concreta eligiÃ³ el usuario.
- **ConclusiÃ³n iOS**: se puede *mostrar* al usuario un informe visual de su uso, pero NO se puede entregar a la app datos crudos exportables para construir un "mapa" propio (menos aÃºn en servidor), ni obtener granularidad de contenido. Un hilo del Apple Developer Forums documenta que reconstruir datos crudos desde la extensiÃ³n es posible pero Apple lo trata como comportamiento no soportado.

### 3. APIs de redes sociales: NINGUNA da uso en tiempo real ni consumo de contenido
- **Instagram**: la Basic Display API se cerrÃ³ definitivamente el **4 de diciembre de 2024**. El anuncio oficial de Meta for Developers (publicado el 4-sep-2024) dice: *"Starting on December 4th, 2024, Instagram Basic Display API will no longer be availableâ€¦ After December 4th, 2024, there will no longer be a set of Instagram APIs for consumer developer apps."* Los desarrolladores tuvieron 90 dÃ­as para migrar. La sucesora (Instagram API with Instagram Login / Instagram Graph API) da contenido y perfil, **no** historial de uso ni tiempo de scroll. No existe API de "Your Activity"/"tiempo en la app" exportable.
- **TikTok**: la Display API solo devuelve datos del propio usuario autenticado (perfil, sus propios vÃ­deos publicados), sin tiempo de visionado ni acceso a la "For You Page". La Research API es exclusiva para investigadores acadÃ©micos/sin Ã¡nimo de lucro aprobados (cuota ~1.000 peticiones/dÃ­a, rechazo habitual de usos comerciales). Existe una **Data Portability API** (2024) que es la vÃ­a oficial mÃ¡s cercana pero limitada (ver Details).
- **YouTube**: el Data API v3 **no tiene** endpoint de historial de visionado. La propiedad `contentDetails.relatedPlaylists.watchHistory` fue deprecada el 11-ago-2016 y devuelve vacÃ­o/placeholder desde entonces; `activities` estÃ¡ deprecado y no hay *scope* de OAuth para historial.
- **ConfirmaciÃ³n explÃ­cita solicitada**: por el *sandboxing* de iOS/Android y por las polÃ­ticas de privacidad y de competencia de estas plataformas, **ninguna red social ofrece una API en tiempo real** que permita a una app de terceros leer cuÃ¡nto tiempo lleva un usuario scrolleando o quÃ© tipo de contenido estÃ¡ consumiendo en ese momento. Esto es una limitaciÃ³n estructural, no un hueco temporal de producto.

### 4. Fuente de datos "reales" mÃ¡s creÃ­ble: los exports GDPR/CCPA
- Instagram "Descarga tu informaciÃ³n" (formato JSON) y TikTok "Download your data" (JSON, no TXT) permiten al usuario obtener su propio historial, y son la vÃ­a oficial y segura (sin ceder contraseÃ±as).
- **LimitaciÃ³n crÃ­tica de granularidad temporal**: estos exports dan autor + timestamp (Instagram: "posts viewed"/"videos watched" incluyen solo autor y hora de visionado, sin identificar la pieza exacta) o fecha + link de vÃ­deo (TikTok). **NO incluyen tiempo de visionado/duraciÃ³n por pieza**, por lo que no permiten reconstruir cuÃ¡nto tiempo se vio cada contenido.

## Details

### Android `UsageStatsManager` â€” inventario preciso
`UsageStats` contiene: `getPackageName()`, `getTotalTimeInForeground()`, `getLastTimeUsed()` y begin/end del intervalo. Datos concretos para el "mapa":
- **Tiempo por app**: SÃ (`getTotalTimeInForeground`).
- **NÃºmero de desbloqueos**: SÃ, inferido de `KEYGUARD_HIDDEN` (cÃ³digo 18, "typically happens when the user unlocks their phone after turning it on") y/o `SCREEN_INTERACTIVE`.
- **Notificaciones recibidas**: PARCIAL. `NOTIFICATION_INTERRUPTION` (cÃ³digo 12) existe pero estÃ¡ marcado `@hide`/`@SystemApi` en el cÃ³digo fuente de AOSP (no oficialmente accesible a apps de terceros); `NOTIFICATION_SEEN` (cÃ³digo 10) estÃ¡ documentado. El conteo fiable de notificaciones para apps de terceros no estÃ¡ garantizado.
- **Franjas horarias de uso**: SÃ, a partir de los timestamps de los eventos.
- **Tipo de contenido intra-app**: NO por esta vÃ­a.

Nota sobre nombres de paquete para el proxy de clasificaciÃ³n: Instagram = `com.instagram.android`; TikTok = `com.zhiliaoapp.musically`; YouTube = `com.google.android.youtube`. Estos permiten mapear "app â†’ tipo de plataforma" (social/vÃ­deo corto/vÃ­deo largo) como proxy de "tipo de contenido a nivel de app".

**Digital Wellbeing**: Google **no** expone una API pÃºblica para exportar datos de Digital Wellbeing a terceros. HistÃ³ricamente las apps de terceros solo podÃ­an crear horarios de "No molestar" con las APIs de Wellbeing; Android 15 abriÃ³ parcialmente funciones (p.ej. Dark Mode, apagar Always-On Display) pero **no** la exportaciÃ³n de datos de uso. La Ãºnica fuente equivalente para terceros sigue siendo `UsageStatsManager`.

**Riesgo emergente con `AccessibilityService`** (Ãºnica vÃ­a tÃ©cnica para inferir contenido intra-app en Android): Google Play solo exime de divulgaciÃ³n a apps que declaran `isAccessibilityTool=true` y cuyo propÃ³sito primario es asistir a personas con discapacidad; el resto debe completar una declaraciÃ³n con justificaciÃ³n y vÃ­deo, y "using the API for general automation or non-accessibility purposes is not allowed". AdemÃ¡s, Android 17 (beta) con Advanced Protection Mode **revoca y bloquea** automÃ¡ticamente el permiso de accesibilidad a apps no clasificadas como herramientas de accesibilidad. Usar AccessibilityService para analÃ­tica de bienestar (no discapacidad) tiene alto riesgo de rechazo/retirada.

### iOS â€” `FamilyControls` / `ManagedSettings` / `DeviceActivity`
`FamilyControls` es la puerta de autorizaciÃ³n; `ManagedSettings` aplica las restricciones (aplicadas por el SO, no por la app); `DeviceActivity` monitoriza (`DeviceActivityMonitor`, eventos por umbral) y renderiza informes (`DeviceActivityReport`). Los informes se filtran por `DeviceActivityFilter` (segmentos `.daily`/`.hourly`, dispositivos, apps/categorÃ­as) y exponen agregados como `totalActivityDuration` dentro de la extensiÃ³n aislada â€” pero esos datos no pueden salir hacia la app anfitriona ni a la red. Esto es intencional y descrito por Apple como "privacy-preserving".

### TikTok Data Portability API â€” el hallazgo mÃ¡s relevante para "datos reales"
Documentada en developers.tiktok.com (anunciada en 2024). Estructura de *scopes* OAuth: `portability.<tipo>.single` y `portability.<tipo>.ongoing`, con `<tipo>` âˆˆ {`all`, `activity`, `directmessages`, `postsandprofile`} (ocho scopes en total). CategorÃ­as: "Posts and Profile", "Activity", "Direct Messages" y "Full Archive"; cada una admite una peticiÃ³n puntual (single) o acceso continuado (ongoing).

**Dato decisivo**: dentro de "Activity", la sub-categorÃ­a **"Watch History" expone exactamente dos campos â€” "Watch History Date" y "Video landing page link" â€” SIN ningÃºn campo de watch time / duraciÃ³n por vÃ­deo.** La Ãºnica duraciÃ³n de visionado que aparece en el archivo es la de las retransmisiones LIVE propias del usuario ("LIVE Duration: start time - end time (duration mins)"), no la del feed. 

Condiciones de acceso (doc oficial): *"Our Data Portability API, available for TikTok users in the European Economic Area (EEA) or United Kingdom (UK), allows users to authorize a transfer of their information to your app."* Es **aprobada por solicitud** (revisiÃ³n de privacidad y seguridad + mockups UX de alta fidelidad; respuesta tÃ­pica en 3-4 semanas), **limitada a usuarios de EEA/UK** (no devuelve datos fuera de esas regiones), requiere consentimiento OAuth del usuario, y es **asÃ­ncrona** (flujo: Add Data Request â†’ Check Status â†’ callback vÃ­a webhook â†’ Download; los datos quedan disponibles 4 dÃ­as). No es una API de tiempo real. Se enmarca en "market contestability"/portabilidad de datos (contexto DMA de la UE, aunque el texto usa "market contestability", no cita literalmente el DMA).

### Terceros que resuelven parte del problema (inspiraciÃ³n tÃ©cnica)
- **RescueTime (Android)** usa exactamente `UsageStatsManager` (permiso "App usage data") para el tracking de tiempo por app, y reserva `AccessibilityService` Ãºnicamente para bloquear apps durante sesiones de foco. Su API OAuth2 ofrece datos agregados por categorÃ­a y nivel de productividad â€” no granularidad de contenido. Es el patrÃ³n tÃ©cnico a replicar en Android.
- **RescueTime en iOS** no puede trackear apps/webs por las limitaciones de Apple; recurre a *location tracking*, lo que confirma la ausencia de una vÃ­a oficial de datos de uso en iOS.
- **Exports de Screen Time en iOS**: los entusiastas extraen datos de la base `knowledgeC.db` (requiere Mac con misma cuenta iCloud, o jailbreak) o vÃ­a Shortcuts + OCR de capturas de pantalla. Ninguna de estas rutas es escalable ni apta para producciÃ³n o para un demo de formaciÃ³n limpio.

## Recommendations

**Etapa 1 â€” MVP/demo (recomendado): Android + `UsageStatsManager` real.** Es la Ãºnica fuente que entrega datos reales de uso por app con permisos estÃ¡ndar del SO. Mapear paquetes conocidos a categorÃ­as de "tipo de plataforma" como proxy de "tipo de contenido". Presentar tiempo por app, desbloqueos y franjas horarias. **Corregir la spec** para dejar explÃ­cito que la clasificaciÃ³n es a nivel de app completa, no intra-app.

**Etapa 2 â€” Enriquecer credibilidad con datos reales del usuario: importaciÃ³n manual de exports GDPR.** AÃ±adir un flujo donde el adolescente descarga su Instagram JSON / TikTok JSON y lo sube a la app; aporta historial real de piezas vistas con timestamps (fuente "real" y no simulada para la demo). Advertir en la UI que no incluye duraciÃ³n por pieza ni tipo de contenido dentro de la app.

**Etapa 3 â€” iOS (solo si es imprescindible): informe visual, no datos crudos.** Planificar exclusivamente un `DeviceActivityReport` con autorizaciÃ³n `.individual`, asumiendo que (a) requiere aprobar el entitlement Family Controls con Apple, (b) no se obtienen datos crudos exportables, (c) no hay granularidad intra-app, (d) el usuario puede desactivar la autorizaciÃ³n individual.

**QuÃ© NO hacer:** no prometer detecciÃ³n de "Reels vs Stories vs DMs" ni lectura en tiempo real del scroll; no depender de APIs de redes sociales para uso/consumo (no existen); no construir el producto sobre `AccessibilityService` por el riesgo de rechazo/baneo en Google Play y el bloqueo en Android 17.

**Umbrales/benchmarks que cambiarÃ­an esta recomendaciÃ³n:** (1) que Apple abra una API de datos de uso accesible fuera de la extensiÃ³n aislada; (2) que Instagram/TikTok/YouTube publiquen endpoints oficiales de "tiempo en la app"/watch-time por usuario final; (3) que se consiga una colaboraciÃ³n/partnership oficial de plataforma con acceso a granularidad intra-app; (4) que la TikTok Data Portability API aÃ±ada un campo de duraciÃ³n de visionado o se extienda fuera de EEA/UK. Cualquiera de estos habilitarÃ­a granularidad intra-app o tiempo real.

## Caveats
- Las polÃ­ticas de plataforma cambian con frecuencia; **toda la informaciÃ³n estÃ¡ fechada a julio de 2026**. Verificar la documentaciÃ³n oficial antes del desarrollo.
- Los hechos clave (deprecaciÃ³n de Instagram Basic Display el 4-dic-2024, campos de la TikTok Data Portability API, nota de permiso de `UsageStatsManager`, restricciones de `AccessibilityService` y Android 17, aislamiento de `DeviceActivityReport`) estÃ¡n corroborados con documentaciÃ³n oficial de Meta, TikTok, Apple y Google; parte del detalle operativo sobre exports proviene de fuentes secundarias (blogs, foros) y se ha marcado como tal.
- El evento `NOTIFICATION_INTERRUPTION` estÃ¡ marcado como oculto/sistema en AOSP; el conteo fiable de notificaciones para apps de terceros no estÃ¡ garantizado y debe validarse en dispositivos reales.
- La cifra de "10.000â€“50.000+ entradas" en exports de TikTok que circula en blogs no estÃ¡ respaldada por fuente oficial y se ha omitido deliberadamente.
- Consideraciones legales adicionales para menores (13-18 aÃ±os): el RGPD y su tratamiento de datos de menores, COPPA (EE. UU.) y las polÃ­ticas de Google Play para apps dirigidas a niÃ±os/adolescentes aplican y requieren revisiÃ³n jurÃ­dica especÃ­fica, especialmente si se procesan datos de uso en servidor.
